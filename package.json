{
  "name": "mailgun-notion",
  "version": "1.0.0",
  "description": "A webhook service to create Notion pages from Mailgun emails",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.18.0",
    "@notionhq/client": "^2.2.14",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "serverless-http": "^3.2.0"
  },
  "scripts": {
    "start": "serverless offline",
    "deploy": "serverless deploy"
  },
  "devDependencies": {
    "serverless-offline": "^14.4.0"
  }
}
